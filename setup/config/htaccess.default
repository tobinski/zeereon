RewriteEngine Off
AuthType Basic
AuthName "eTempus"
AuthUserFile {auth_file_place}
ErrorDocument 401 "<h1>401 - eTempus- Zugriff verweigert</h1>eTempus kann nur mit einem gueltigen Login verwendet werden."
require valid-user
